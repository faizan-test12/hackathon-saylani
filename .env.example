# Roast & Co. environment variables
# Copy this to .env and fill in your values

SECRET_KEY=change-me-to-a-random-string
GOOGLE_API_KEY=add your file

# SQLite is the default — no DATABASE_URL needed unless switching to Postgres
# DATABASE_URL=postgresql://user:pass@localhost/roastco
